name: workflow_variables
on: 
    workflow_dispatch
env:
    cloud: aws
jobs:
    greeting_job:
      runs-on: ubuntu-latest
      env:
        Greeting: Hello
      steps:
        - name: "Its a Greeting Job"
          run: echo "$Greeting $First_Name is using $cloud!"
          env:
            First_Name: Swarup


